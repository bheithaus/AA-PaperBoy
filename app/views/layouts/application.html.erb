<!DOCTYPE html>
<html>
<head>
  <title>PaperBoi</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <header class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to "PaperBoi", root_path, id: "logo" %>
          <nav>
            <ul class="nav pull-right">
              <li><%= link_to "Newspapers", newspapers_path %></li>
              <li><%= link_to "Sign Up", "" %></li>
            </ul>
          </nav>
        </div>
      </div>
    </header>

    <div class="container">
      <%= render partial: 'layouts/show_flashes' %>
      <%= yield %>
  </body>
</html>
